mixin card(title, paragraph1, paragraph2, buttonLink, buttonText, buttonType)
    .card
        if title
            h3 #{title}
        if paragraph1
            p #{paragraph1}
        if paragraph2
            p #{paragraph2}
        if buttonLink && buttonText
            +button(buttonLink, buttonText, buttonType)